export type UserDb = {
    id: string,
    name: string,
    email: string,
    age: number
};

export type ProductDb = {
    id: string,
    name: string,
    description: string,
    price: number
};